{% extends 'base.html.twig' %}

{% block title %}Profil
{% endblock %}

{% block body %}
	<section style="margin-left:50px; display:block;">
		<h1>Mon profil</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ user.username }}</td>
				</tr>
				<tr>
					<th>Email</th>
					<td>{{ user.email }}</td>
				</tr>

			</tbody>
		</table>


		<i class="fa-solid fa-arrow-rotate-left"></i>
		<a href="{{ path('user_index') }}">retour sur les posts</a>


	</br>
	<i class="fa-solid fa-pen-to-square"></i>
	<a href="{{ path('user_edit', {'id': user.id}) }}">modifier mon profil</a>
</br>
<i class="fa-solid fa-house-user"></i>

<a href="{{ path('app_home') }}">retour à la page d'accueil</a></br>{{ include('user/_delete_form.html.twig') }}</section>{% endblock %}
